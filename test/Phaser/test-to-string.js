describe('Phaser TEST', function() {

    describe('Phaser.prototype.toString', function() {

        it('should return "[SoundModule Phaser]"', function() {
            expect(phaser.toString()).toEqual('[SoundModule Phaser]');
        });

    });

});
