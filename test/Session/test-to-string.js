describe('Session TEST', function() {

    describe('Session.prototype.toString', function() {

        it('should return "[SoundModule Session]"', function() {
            expect(session.toString()).toEqual('[SoundModule Session]');
        });

    });

});
